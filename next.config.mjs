/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'aceternity.com',
            'images.unsplash.com',
            'cdn.cvs0.xyz',
            '5skn.xyz'
        ]
    }
};

export default nextConfig;
